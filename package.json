{
  "name": "cork-event-radar",
  "version": "1.0.0",
  "description": "A Cork and West Cork event discovery dashboard with local source scanning.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js && node tests/source-routing.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}
